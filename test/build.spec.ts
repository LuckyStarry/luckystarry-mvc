/* tslint:disable */
import { expect } from 'chai'
import { Build } from '../src/build'

describe('/build.ts', function() {
  it('存在 Build Install', function() {
    expect(typeof Build).to.equal('function')
  })

  it('存在 Build Install', function() {
    expect(typeof Build).to.equal('function')
  })
})
